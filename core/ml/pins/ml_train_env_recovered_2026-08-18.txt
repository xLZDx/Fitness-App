# D:/tools/ml-train-env -- RECOVERED, not authored
#
# Captured 2026-08-18 with pip freeze against the venv that produced the v1
# scanner artefacts. Read the provenance before you read the versions.
#
# This is a RECOVERY pin and not the historical one. No pin file of any kind
# existed when v1 was trained, so nothing here proves these were the versions
# in use on 2026-07-29. It proves only that these are the versions in the
# environment that survives, and that this environment reproduces v1's
# headline metric -- FINAL val_accuracy=0.617, matching the registry. See
# core/ml/SCANNER_PROVENANCE.md for that run.
#
# The gap this closes is narrow and worth stating exactly. Until now the
# versions existed in one directory on one machine and nowhere else, so a
# reinstall, a disk failure or one unattended upgrade would have destroyed the
# only record of what reproduces v1. The list now survives the environment.
#
# What it does NOT do is make the historical claim recoverable. "These were
# the versions v1 was trained with" remains UNKNOWN, and writing a file does
# not change that. Recording a recovered environment as though it were the
# original pin is exactly the substitution this repository refuses.
#
# Environment: Python 3.11.9, Windows, no CUDA, no GPU visible.
# Packages: 58
# sha256 of the package list below: 6ae6e7dbe41f9979daaa237c56334f00802ac43e5f6b68ffce808893c47e6287
#
absl-py==2.5.0
astunparse==1.6.3
beautifulsoup4==4.15.0
bs4==0.0.2
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
contourpy==1.3.3
cryptography==49.0.0
cycler==0.12.1
flatbuffers==25.12.19
fonttools==4.63.0
gast==0.7.0
google-auth==2.56.2
google-auth-oauthlib==1.4.0
google-pasta==0.2.0
grpcio==1.83.0
h5py==3.16.0
icrawler==0.6.10
idna==3.18
keras==2.15.0
kiwisolver==1.5.0
libclang==18.1.1
lxml==6.1.1
Markdown==3.10.2
MarkupSafe==3.0.3
matplotlib==3.11.1
mediapipe==1.0.0
ml-dtypes==0.3.2
numpy==1.26.4
oauthlib==3.3.1
opencv-contrib-python==5.0.0.93
opt_einsum==3.4.0
packaging==26.2
pillow==12.3.0
protobuf==4.25.9
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
requests==2.34.2
requests-oauthlib==2.0.0
six==1.17.0
sounddevice==0.5.5
soupsieve==2.9.1
tensorboard==2.15.2
tensorboard-data-server==0.7.2
tensorflow-cpu==2.15.1
tensorflow-estimator==2.15.0
tensorflow-intel==2.15.1
tensorflow-io-gcs-filesystem==0.31.0
termcolor==3.3.0
typing_extensions==4.16.0
urllib3==2.7.0
Werkzeug==3.1.8
wrapt==1.14.2
