# Backlog — operator dump of 2026-07-31

Raw input: 23 numbered items + a structured note on onboarding-driven ranking + two
screenshots of the live form-check screen. This file is the working container: the raw items are
deduplicated into epics, each epic states what is **verified**, what is **assumed**, and what
**blocks** it.

Nothing here is approved. Each gate needs its own `GO`.

Source of the raw list: chat, 2026-07-31 ~04:10 local (Europe/Chisinau) / 01:10 UTC.

---

## Map: raw item → epic

| Raw | Short | Epic |
|---|---|---|
| note, 3, 11, 12, 13, 14, 15, 20 | onboarding, gender/level/location, goal-first home | **E1 Personalisation** |
| 2, 4, 7, 8, 18 | AR, live coach, reference comparison, recording, review | **E2 Virtual trainer** |
| 6, 18 | scan latency, coach hung, English leaking | **E3 Defects (now)** |
| 19, 22 | ready-made programmes, outdoor/park equipment | **E4 Content** |
| 5, 8b, 9 | trending, challenges, community chat | **E5 Social** |
| 16, 21, 23 | full functional test coverage, agent review, design | **E6 Quality** |
| 17 | ask whenever there is doubt | **rule, applies to me** |

---

## E3 — Defects, verified now (highest priority: these are live on the operator's phone)

> **E3.1 and E3.2 are SHIPPED — re-verified against the code 2026-08-08.** The
> two sections below describe the defects as they were on 2026-07-31 and are
> kept for the reasoning, not as open work. Checked before starting a gate to
> fix them, which is how the staleness was found:
>
> * **E3.1 fixed.** `form_classifier.dart` no longer holds sentences at all —
>   it emits a `FormCueKey`, and `cue_text.dart:24`
>   (`formCueText(AppLocalizations l10n, FormCueKey)`) resolves all nine keys
>   through l10n. The file's own header states the rule: keys "cannot
>   localise; they name the cue and the UI/speech boundary resolves it".
> * **E3.2 fixed.** `pose_gate.dart` exists and `evaluateGated`
>   (`form_classifier.dart:311`) drops unscorable frames before any classifier
>   sees them — `:354` states it as "there is no body in the picture".
>
> Not re-verified here: whether the *wording* of every cue is good, and
> whether the gate's thresholds are right on a real device. Those are
> calibration, not the defects recorded below.

### E3.1 English leaking into the Russian UI of "Тренер по технике" — VERIFIED

| Symptom | Source |
|---|---|
| `Half-rep. Lighten the bar and hit depth.` spoken + shown | `lib/features/form_check/data/form_classifier.dart:57-146` — 9 cue strings, hardcoded English |
| `Ошибки: squat.depth, deadlift.back_angle, pushup.alignment` | `form_classifier.dart:38,81,118` — internal rule ids rendered as user text |
| `Free of $2,500 hardware. Works on your phone...` | `form_check_page.dart:373` |

**Why the round-4 guard missed it:** `test/l10n/no_untranslated_strings_test.dart` scans for English
literals inside `Text(...)` calls. These are values of a `cue:` field on a data class and an
identifier getter — structurally invisible to that test. The guard needs a second rule: no English
sentence in any string field that reaches a `VoiceCoach` or a UI label, and rule ids must never be
rendered.

### E3.2 The coach scores a face — VERIFIED symptom, HYPOTHESIS cause

Screenshot: `0 чисто — 6 с ошибками (всего 6)` produced from a front-camera selfie of a face at
arm's length. No squat, no full body in frame.

Hypothesis (to confirm on the gate, not asserted): nothing gates classification on landmark
visibility or on the body being in frame, so ML Kit's low-confidence landmarks for a face-only frame
are fed to `SquatDepthClassifier` / `DeadliftBackAngleClassifier` / `PushupAlignmentClassifier`
anyway. `CueGate` then behaves exactly as designed — severity ≥ 2 repeats every `interruptGapMs`
(1200 ms) while the "error" persists (`voice_coach.dart`, the interrupt branch) — which is what the
operator experienced as "завис, повторяет одни и те же слова".

Fix shape: an in-frame / visibility gate BEFORE classification, and a visible "поставьте телефон
так, чтобы было видно всё тело" state instead of scoring garbage. This is also the precondition for
E2 — a virtual trainer that scores a face is worse than no trainer.

### E3.3 Scan latency — NEEDS OPERATOR INPUT

Operator reports 65–180 s. Code has a hard 20 s deadline with on-device fallback
(`gemini_equipment_service.dart:37`, shipped in 1.0.0+8). **Blocked on: which build was tested.**
If +8 already, the delay is elsewhere (resize, on-device model load, or network) and needs a
device-side trace, not another timeout.

---

## E2 — Virtual trainer (the AR ask: items 2, 4, 7, 8, 18)

### What "дополненная реальность" means here

Not ARCore world-anchoring. The value the operator describes — see yourself, see what is wrong,
hear the correction — is delivered by a **skeleton overlay drawn on the live camera preview**, with
per-segment colouring. That is achievable on-device with what is already installed, at a fraction of
the cost, and works on phones ARCore does not support.

### Building blocks already present (verified)

| Piece | Where |
|---|---|
| On-device pose, 33 landmarks | `google_mlkit_pose_detection: ^0.14.0`, `pubspec.yaml:58` |
| Speech out | `flutter_tts: ^4.2.2`, `:62` |
| Speech in | `speech_to_text: ^7.0.0`, `:61` |
| Cue throttling policy (tested, pure) | `form_check/data/voice_coach.dart` — `CueGate` |
| TTS adapter with failure surfacing | `form_check/data/tts_voice_coach.dart` |
| Rep counting | `form_check/data/rep_counter.dart` |
| Muscle atlas, 107 named ids, CC BY 4.0 | `assets/anatomy/muscle_front.svg`, `muscle_back.svg` |
| 192 exercises with 2 stills each | `assets/data/exercises.json` |

### The reference-form problem

To say "your arm is wrong" the app needs an **эталон** to compare against. Proposal: extract joint
angles offline (build-time `mediapipe` script, same model family as ML Kit) from the two catalog
stills per exercise → a start-pose and end-pose joint-angle vector → the reference envelope.

**Honest limitation, stated up front:** two stills give the two endpoints of a rep, not the path
between them. That is enough to judge *depth, alignment and end positions*, and not enough to judge
*tempo or mid-path deviation*. Anything stronger needs real reference video, which we do not have a
free licensed source for. This limitation must be visible in the product, not hidden — the app
should say what it is checking.

### Single RGB camera, no depth — named failure modes

| Failure | Detection instead of garbage |
|---|---|
| Camera angle wrong (front view for a squat) | per-exercise required view; detect shoulder/hip width ratio and ask the user to turn the phone |
| Partial body in frame | landmark visibility gate (E3.2) |
| Torso rotation ambiguity | flag low confidence, drop to "cannot judge this rep" rather than scoring |

### Gate breakdown

| Gate | Content |
|---|---|
| **V0** | E3.1 + E3.2 — localise cues, hide rule ids, gate on visibility. Prerequisite for everything below |
| **V1** | Offline `mediapipe` extraction → `assets/data/pose_reference.json` (start/end joint angles per exercise) |
| **V2** | Live skeleton overlay on the camera preview, per-segment colour (ok / drift / danger) |
| **V3** | Segment-level Russian cues through the existing `CueGate` — "разверни локоть", "колено внутрь" |
| **V4** | Session recording + post-workout replay with error markers on a timeline |
| **V5** | Per-exercise technique score, history, and the challenge/leaderboard layer (item 8b) |

**V4 carries a promise conflict — must be resolved before it is built.** The app currently tells the
user, on that exact screen: *«Кадры никуда не загружаются — камера остаётся приватной»*. Recording
workouts and/or sending them for cloud analysis contradicts that sentence. Options: keep recordings
strictly on-device (promise survives), or change the promise and add an explicit consent step. This
is an operator decision, not a technical one.

---

## E1 — Personalisation (items note, 3, 11, 12, 13, 14, 15, 20)

Requirement, condensed: the home screen leads with **the user's goal and distance to it**; content is
ranked by **gender, level/intensity, and location (home / gym / outdoor)**; a detailed questionnaire
(10 mandatory + unlimited optional) feeds the ranking; ranking, not hard filtering.

Open design questions the operator already raised himself, kept verbatim in intent:

1. What ranking applies if the user skips the questionnaire?
2. How is priority split on multi-select (Home + Outdoor)?
3. What happens to the feed when preferences change a month later?
4. Hard filter (100 % chosen location) or proportional mixing (e.g. 80/20) for discovery?

**Known blocker:** the catalog has no location, intensity or gender tags today — ranking cannot be
built before the 192 exercises + 48 machines carry them. Sizing that is part of the architecture
review running now.

Gender segmentation additionally needs a **female anatomy variant** of the atlas; the current
`muscle_front.svg` / `muscle_back.svg` is a single body.

---

## E4 — Content (items 19, 22)

**19 — ready-made programmes** for weight loss, posture, balance, endurance, strength, flexibility,
suggested from the questionnaire.

Two corrections to the ask, stated plainly:
- "100 % эффективные" — no training programme is 100 % effective for everyone; the honest version is
  *evidence-based* programmes built on published public guidelines (ACSM / WHO physical-activity
  guidance), with expected ranges, not promises.
- "скопируй" — copying a commercial programme verbatim is a copyright problem. Build from
  public-domain guidance instead. Same discipline that governs the exercise catalog.

**22 — outdoor / park equipment** recognition: horizontal bars, parallel bars, stairs, monkey bars,
outdoor benches. This extends the existing 48-machine registry and the alias index; the recogniser
already handles unknown-machine fallback, so this is catalog work plus alias entries, not new
architecture. *(Partly done 2026-08-04: `outdoor_air_walker` is in the registry with 3 exercises —
see `core/EQUIPMENT_REGISTRY_EXPANSION_2026-08-03.md`. Bars/stairs/monkey bars still open.)*

### E4.1 — the four machine pages with no footage — DEFERRED 2026-08-04

Operator, 2026-08-04: *"4 пустые страницы тренажёров - если нет ролика то надо найти в интернете
картинку тренажеров и прекрепть к ним с минимальным наботом упражнений"*, then *"отложи 2 задачу в
бэклог, потом будем решать"*.

`recumbent_bike`, `glute_kickback_machine`, `t_bar_row` and `rotary_torso_machine` are real machines
with real pages and **zero exercises** — the purchased library has no clip for any of them
(confirmed by hand, `core/VENDOR_EQUIPMENT_LINK_2026-08-03.md`). The page currently reads "нет
подобранных упражнений — спросите AI-тренера ниже", which is a dead end: the AI coach below it
generates text-only exercises that `withDemonstration` then filters out, so nothing appears.

**Why this was not just built.** Both halves of the ask collide with decisions already made, one of
them by the operator in the same session:

1. *An internet photograph on the machine header* reverses 2026-08-03's *"фото должно быть превью
   ролика"*. The header was deliberately changed from a photograph to a frame of the machine's own
   licensed clip, because a photograph next to 3D renders was the same inconsistency the clip-only
   rule exists to end (`equipment_providers.dart`, `equipmentHeroImageProvider`). It is also a
   licence question: this whole catalog was just cleaned of unlicensed footage.
2. *A minimal exercise set* cannot be shown at all without clips. `recommendedExercisesProvider`
   applies `withDemonstration`, per *"я просил их всех убрать чтобы было все одинаково"*. Hand-
   authored or AI-generated entries are invisible there by design — already asserted in
   `ai_fallback_provider_test.dart:112`.

**Options, for the decision that is still open:**

- **(a) Reuse what H5 already built** — replace the dead-end copy with the "контент готовится" card
  plus the outside-video link, which exists and ships today. No new images, no licence exposure, no
  inconsistency. Smallest change; does not add exercises.
- **(b) Licensed photographs** — Wikimedia Commons / Openverse (the operator's own suggestion,
  2026-08-03), CC attribution on the existing `/licences` screen where the anatomy chart is already
  credited. Needs a new image field on `EquipmentItem` and a candidate list reviewed before commit.
- **(c) Buy or film the four clips** — the only option that makes these pages equal to the other 65,
  and the only one that lets exercises appear at all.

(a) and (b) compose; (c) makes both unnecessary.

### E4.2 — 1 235 недостающих роликов: вендор ответил, паритета по расписанию не будет

Оператор отправил Exercise Animatic аудит покрытия. Ответ получен 2026-08-06.

**Что вендор сказал.**

- Разрыв **не связан** с лицензией или другим тарифом. Ultimate Bundle уже содержит всю доступную
  библиотеку: по их FAQ более 1 700 мужских роликов и более 750 женских.
- Причина историческая: производство начиналось с мужского персонажа, женский добавили позже.
- Расширение женской библиотеки — один из текущих приоритетов производства, долгосрочная цель —
  версии обоих полов для каждого упражнения.
- **Кастомный заказ на 1 235 роликов не принимают.** Ни цены, ни сроков, ни обязательства
  выполнить список. Формулировка: pre-produced stock content, а не анимация под заказ.
- Заявки на недостающие движения рассматривают и могут включить в будущие партии — **без гарантии**,
  что каждый запрошенный ролик будет сделан.
- Новые ролики, вышедшие в рамках их обычного графика, попадают в Master Folder **бесплатно** и
  покрываются той же пожизненной неисключительной лицензией. Отдельной платы не будет.
- Стиль новых файлов останется узнаваемым, но **технически идентичным прежним не будет**: модели,
  свет, кадрирование и рендер у них продолжают меняться.
- Процесс: подавать по одной группе мышц, начиная с Ног, затем Плечи и Спина. Оценивать всё сразу
  они не станут.

**Что от нас нужно** — Excel по одной группе с колонками: наш внутренний идентификатор, точное
название упражнения, имя имеющегося файла (мужского или женского), какого пола версия отсутствует,
примечания по вариации, плюс ссылка на демонстрацию там, где название неоднозначно.

Отдельная оговорка вендора: имена файлов и названия упражнений у них периодически обновляются,
поэтому часть движений может уже существовать под другим именем. Они предлагают либо прогнать
список через поиск в Dropbox и «Find Your Exercise» на сайте до отправки, либо сверить его на своей
стороне («Our team can then review the list against the latest Ultimate Bundle library»). Из
репозитория это не проверяется — состояние их библиотеки нашими данными не описывается.

**Что у нас уже есть.** `core/vendor_clip_gaps_2026-08-05.csv` — 1 235 строк, колонки
`exercise_id, title_en, title_ru, group, primary_muscles, equipment_id, difficulty,
missing_version, existing_reference_clip`. Отсутствует женская версия в 1 112 строках, мужская — в
123. По группам: Legs 222, Shoulders 189, Abdominals 131, Back 128, Chest 123, Biceps 117,
Calisthenics-Cardio-Plyo-Functional 91, Stretching-Mobility 86, Triceps 73, Yoga 33, Forearms 21,
Powerlifting 21.

**Задача (СОБРАНА 2026-08-06, ждёт отправки оператором):** `core/vendor_requests/legs_2026-08-06.xlsx`
— 222 строки, 202 без женской версии, 20 без мужской. Рядом CSV-двойник (для диффа) и
`.md` — сопроводительное письмо, которое можно вставить в ответ вендору.

Генератор: `scripts/catalog/build_vendor_request_batch.py --group Shoulders --write` соберёт
следующую партию в том же формате; 11 тестов в `test_build_vendor_request_batch.py`.

Три решения, которые стоит знать до отправки:

- **Колонок ровно шесть**, как вендор и просил. Equipment и difficulty есть у нас, но не
  экспортируются: название уже несёт вариацию («Barbell Bulgarian Split Squat Left Side View»),
  а лишняя колонка — это вопрос, на который читающему придётся решать, реагировать или нет.
- **`Variation Notes` пуст, и это измерено, а не предположено.** Внутри партии ни одно название
  не совпадает с другим — скрипт проверяет на каждом запуске и предупредил бы.
- **Имя файла, а не наш путь.** В исходном CSV лежит `exercises/men/Legs/<name>.mp4`; каталоги —
  наша раскладка, искать в их Dropbox по ней нечего. Отдельный тест на то, что в выгрузке нет
  ни одного слэша.

Оговорка вендора про переименования **не проверена и не могла быть**: состояние их библиотеки
нашими данными не описывается. Сопроводительное письмо говорит это прямым текстом — список
подан как «отсутствует по нашей копии Master Folder», а не как выверенный.

**Следствие, которое важнее самой отправки.** Паритет придёт в чужой очереди без сроков и без
гарантий, то есть к запуску его не будет. Значит вопрос **«что показывать женщине, когда женского
ролика нет»** — наш продуктовый вопрос, а не ожидание поставки, и решать его надо независимо от
этой заявки. Он остаётся открытым.

---

## E5 — Social (items 5, 8b, 9)

- **5** — "what's trending" section on the home screen. Needs a defined, honest data source: real
  in-app usage is the only source we can actually measure; "отзывы в интернете" cannot be sourced
  without a data pipeline we do not have. Proposal: rank by in-app completion counts, labelled as
  such.
- **8b** — challenges keyed to questionnaire level, cross-user comparison at equal physical level.
- **9** — community chat by region / country / city / gym.

**9 needs moderation and privacy design before code.** A public chat exposing city + gym is a
safety surface, especially with a mixed-age user base. Not a reason to skip it — a reason to design
it deliberately.

---

## E6 — Quality (items 16, 21, 23)

- **16** — full functional coverage: one consolidated high-level workflow script plus per-element
  functional scripts covering every card, menu, description, guide and action. Current state: 721
  host tests, 1 integration test (`integration_test/app_test.dart`). The gap is the on-device layer —
  the camera, ML Kit and TTS paths that host tests structurally cannot see.
- **21 / 23** — review through agents. First panel launched 2026-07-31: flutter-reviewer
  (live-coach defects + UX), security-reviewer (camera/recording/chat privacy), code-architect (AR
  blueprint), architect (home screen + onboarding gap analysis). Findings to be appended here.

---

## Rule captured from item 17

> «всегда ори если что то не понятно или есть хоть 0.000001 % сомнений — спроси у меня, шаг за
> шагом всё поправим»

Applies to me, permanently: when a requirement admits two readings, ask before building, and say
which reading I would pick. Reinforces the existing No-Guessing rule — this one extends it from
*facts about system state* to *ambiguity in the requirement itself*.

---

## Blocking questions (nothing starts before these)

1. **Build under test** — was the 65–180 s scan seen on 1.0.0+8, or an earlier build?
2. **Order** — fix the live coach first (E3/V0), or start personalisation (E1)?
3. **Recording privacy** — recordings stay strictly on-device, or move to cloud analysis with a new
   consent screen and a rewritten privacy promise?
4. **Ranking balance** — hard filter by location, or proportional mixing?
5. **Translation strategy for the catalog** (carried over from the 2026-07-30 plan) — hand-written
   for a narrow set, or batch-drafted via Gemini with spot-checks?

---

## E7 — Готовность к публикации в Play (отложено оператором, 2026-08-05)

Гейт P0 закрыт и запушен: политика и Условия настоящие, опубликованы на
`fitness-app-korostelev.web.app/privacy` и `/terms`, ответы для формы Data
safety выведены из кода (`core/PLAY_DATA_SAFETY_2026-08-05.md`), бэкенд
больше не заявляет 501(c)(3). Ниже — то, что осталось открытым и отложено.

### E7.1 Хранение данных в Firebase AI Logic — НЕ ПРОВЕРЕНО

**Verified:** сканер шлёт JPEG в Gemini (`gemini_equipment_service.dart:37-48`);
это приложение фото не хранит, сохраняется только id распознанного тренажёра
(`firestore_recognition_history.dart:41`).

**Assumed:** что Google не удерживает входные изображения на текущем тарифе.
Именно от этого зависит галочка «Data is processed ephemerally» в форме Data
safety (`PLAY_DATA_SAFETY_2026-08-05.md`, раздел «Photos and videos»).

**Blocks:** заполнение формы Data safety. Если удержание есть — галочку надо
снять, а не защищать. Проверяется по условиям тарифа проекта
`fitness-app-korostelev`; могу выяснить сам.

### E7.2 SHA-256 ключа Play App Signing — НЕВОЗМОЖНО ДО ЗАГРУЗКИ

**Verified:** upload-ключ зарегистрирован в App Check; enforcement включён
(гейт F0·6).

**Blocks:** ключ Play App Signing физически не существует до первой загрузки
бандла. Google переподписывает APK своим ключом, поэтому без второго
отпечатка App Check начнёт отвергать запросы настоящих пользователей из
магазина. Порядок жёсткий: загрузить `app-release.aab` -> Play Console ->
Setup -> App signing -> скопировать SHA-256 -> добавить **вторым**, не
заменяя upload-ключ.

**Внешняя блокировка:** аккаунт разработчика на проверке личности у Google
(со слов оператора, 2026-08-05, «may take a few days»).

### E7.3 F1 — миграция Stripe на Basil — ОТДЕЛЬНЫЙ ГЕЙТ

**Verified:** `index.ts:667` (`inv.subscription`) и `index.ts:526-527`
(`s.current_period_end`) ломаются на API-версии 2025-03-31 и новее. Следствие
не «ошибка», а тихая порча: платящий подписчик запишется как `free`.

**Blocks:** деплой остальных Cloud Functions. Из-за этого гейт P0·1 разворачивал
функцию точечно (`--only functions:generateAnnualReceipt`) — общий деплой
раскатал бы сломанные пути.

**Требует:** SDK 18.x, переписать оба обращения, обновить тесты, реальный
тестовый платёж.

### Побочно: E3/пункт 3 блокирующих вопросов больше не в вакууме

Вопрос «записи остаются на устройстве или уходят в облако» теперь имеет
опубликованный ответ по текущему состоянию: политика прямо утверждает, что
фото прогресса не покидают телефон, а кадр сканера уходит в Gemini. Любое
изменение здесь требует правки `scripts/legal/legal_text.py` и передеплоя
Hosting — текст перестал быть свободной переменной.
