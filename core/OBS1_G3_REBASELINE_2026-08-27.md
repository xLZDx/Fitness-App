# OBS-1 re-baseline evidence ledger — MVP1.G3, Step 0

Base: `1a8bf816ce7c1bd976ed4143d57d3761fbd64527` (closed G2 HEAD). Every original OBS-1 item from
`core/MASTER_PLAN_2026-08-26.md` §8 (13 items) checked against current repository state, not
inherited from the master-plan's own text, per Rosetta plan `Fitness_App-2026-08-27T00-46-51-806Z-3135e2`
Step 0 DoD (GPT-PM, 2026-08-27): "old claim | current repository reality | disposition | replacement
if any | evidence."

| # | Old claim (§8) | Current repository reality | Disposition | Replacement | Evidence |
|---|---|---|---|---|---|
| 1 | [RUNTIME] Auth/data-flow canary -- design only, not built | Zero scheduled Cloud Functions (`onSchedule`/pubsub cron) anywhere in `functions/src`. Only cron in repo is the CI nightly job in `.github/workflows/functions.yml:26-27` (npm audit/build/rules, not a live probe). No sign-in+Firestore canary exists anywhere. | **OPEN** | -- (item 9 of this gate) | Repo-wide search for `onSchedule`/`pubsub.schedule`/`cron`/`canary`/`healthcheck`: zero hits outside CI config and real app auth code. |
| 2 | [CI] Functions test-health enforcement -- design only | `.github/workflows/functions.yml:68,72` runs `npm ci` (no `\|\| true`, no `continue-on-error` anywhere in the file) then `npm test` -> raw `jest` (`functions/package.json:11`), no `--passWithNoTests`. Jest's own default (exit 1, "No tests found") already applies if the suite is empty/skipped. | **PARTIAL** -- structurally fails-closed already, but has never been proven with a positive+negative fixture, and "expected test count" is not explicitly asserted (only Jest's own default behavior is relied on). | -- (item 1 of this gate) | `.github/workflows/functions.yml:68,72`; `functions/package.json:11`. |
| 3 | [RUNTIME] Enforcement-status dashboard -- design only | `scripts/dev/production_manifest.py:230-283` covers App Check enforcement and Firestore rules/ruleset id per service, but **not** Identity Toolkit (`grep` for "Identity Toolkit" in the script: empty). Not scheduled anywhere (`grep -rn` for the script in `.github/workflows/`: empty) -- manual-only, per its own "re-run it, do not edit it" doc comment. Output (`core/PRODUCTION_MANIFEST.md`) last generated 2026-08-12 -- stale. | **PARTIAL** -- 2 of 3 named services covered, not scheduled, output stale. | -- (item 3 of this gate) | `scripts/dev/production_manifest.py:230-283`; `core/PRODUCTION_MANIFEST.md:3`. |
| 4 | [RUNTIME] Billing integrity monitor -- design only | Stripe API version is a single hardcoded literal (`functions/src/index.ts:271`), no comparison/alert against the webhook-configured version. `reconcileDuplicateSubscriptions` (`index.ts:1862-1917`) runs inline in the webhook handler, logs via `logger.warn`/`error` only -- no alerting/paging, no independent scheduled check. Unit tests exist (`stripe_webhook.test.ts`, `stripe_field_moves.test.ts`) but are not monitors. | **OPEN** | -- (item 4 of this gate) | `functions/src/index.ts:271,1862-1917,1884,1904,1912`. |
| 5 | [CI] RU/EN semantic-drift spot-check -- rescoped from "content-parity CI check"; structural parity already CI-enforced | Confirmed: `exercise_translations_test.dart`, `catalog_description_coverage_test.dart`, `catalog_corrections_test.dart` all exist and run in `flutter.yml` (structural: id coverage, step-count match, Cyrillic presence, pinned-row regression). None contain any "semantic"/"meaning"/"mistranslat" logic (grepped, zero matches) -- a translation that preserves step count and Cyrillic but changes meaning is not caught by anything today. 1,368/1,887 rows still unreviewed per master-plan §4 (not independently re-verified this step; carried from existing master-plan evidence). | **OPEN** (structural half ALREADY_CLOSED, semantic half genuinely open) | -- (item 7 of this gate) | `mobile/test/features/equipment/{exercise_translations_test,catalog_description_coverage_test,catalog_corrections_test}.dart`; grep for semantic-related keywords: 0 matches. |
| 6 | [CI] Mobile/Dart dependency (SCA) scanning -- new item, security review | No `dependabot.yml`/`renovate.json` in the repo root. `.github/workflows/flutter.yml` runs `flutter pub get`/`flutter analyze`/`flutter test` only -- no `pub outdated`, `osv-scanner`, `snyk`, or any vulnerability-scanning step. Backend already has `npm audit` (`functions.yml:118-144`); no Dart-side equivalent exists anywhere. | **OPEN** | -- (item 2 of this gate) | `.github/workflows/flutter.yml` (full file checked); repo root (no dependabot.yml/renovate.json); grep for `pub outdated\|osv-scanner\|snyk\|dependabot`: 0 hits. |
| 7 | [RUNTIME] Data-lifecycle sweep verification -- design only | Deletion/export collection lists are hardcoded (`functions/src/index.ts:1989-1992`; `functions/src/account_export.ts:63,93`). Zero uses of `listCollections` anywhere in `functions/src`, `scripts/`, `core/` -- nothing compares the hardcoded list against Firestore's actual live collection set. CI tests deletion *behavior* against the hardcoded list (`functions.yml:88-108`, `account_deletion.e2e.test.ts`), which proves the list deletes what it targets, not that the list is complete. | **OPEN** | -- (item 5 of this gate) | `functions/src/index.ts:1989-1992`; `functions/src/account_export.ts:63,93`; grep for `listCollections`: 0 hits. |
| 8 | [CI] Safety-invariant regression guard -- new item, architect review | `SafetyContext` is `required` at `plan_builder.dart:33` (`required SafetyContext safety,`) and `programme_builder.dart:274` (constructor keyword; field decl at `:288`, non-nullable) -- master-plan's own line citations verified accurate/close. Existing tests (`plan_builder_test.dart`, `programme_builder_test.dart`, `eligibility_test.dart`, `f014_professional_guidance_test.dart`) would catch a dropped `required` at compile time and exercise unsafe-filtering behavior, but none of them is framed as an explicit, permanent **invariant regression test** naming this specifically as its purpose. | **PARTIAL** -- the invariant already holds structurally (Dart's own type system enforces `required`), but no test exists whose stated job is "prove this can never regress," and no test targets an attempted bypass explicitly. | -- (item 3 of this gate) | `mobile/lib/features/ai_planner/data/plan_builder.dart:33`; `mobile/lib/features/programmes/data/programme_builder.dart:274,288`. |
| 9 | [RUNTIME] Gemini/Firebase AI Logic client-direct call monitoring -- new item, security review, premised on `gemini_equipment_service.dart` calling Gemini directly with no Cloud Functions intermediary | **STALE.** G1 (closed 2026-08-26, this session) migrated all 4 AI call sites off direct client calls. `gemini_equipment_service.dart:67-75` now routes through `httpsCallable('aiEquipmentRecognition')`. Repo-wide grep for `package:firebase_ai`, `GenerativeModel(`, `.generateContent(` in `mobile/lib/`: **zero matches**. `firebase_ai: 3.3.0` remains in `mobile/pubspec.yaml:94` as an unused leftover dependency, not a call site. | **STALE / REPLACED** | Server-side AI Gateway observability for the 4 G1 callables (`aiCoachAdvice`, `aiEquipmentRecognition`, `aiMachineDescription`, `aiExerciseGeneration`) -- this gate's item 9. | `gemini_equipment_service.dart:23,67-75`; `ai_coach_service.dart:14,52`; `ai_exercise_generator.dart:39`; `machine_describer.dart:87`; grep for direct-Gemini call patterns in `mobile/lib/`: 0 matches. |
| 10 | [CI] `kCanonicalMachines` <-> `equipment.json` parity check -- new item, architect review, premised on `gemini_equipment_service.dart:143` still being a hardcoded second source of truth | **REPLACED.** `kCanonicalMachines` no longer exists in `mobile/lib/` at all (only a comment noting its past deletion, `gemini_equipment_service.dart:23`). The duplication moved server-side: `functions/src/ai_equipment_recognition.ts:67-89` `CANONICAL_MACHINES`, 69 entries; `mobile/assets/data/equipment.json`, 69 entries. Counts match but no automated content-level (id/name) parity check exists between them -- count parity alone does not prove list identity. | **REPLACED** (mechanism re-based, not closed) | Content-level (not just count) parity check, `CANONICAL_MACHINES` vs `equipment.json` -- this gate's item 4. | `functions/src/ai_equipment_recognition.ts:67-89`; `mobile/assets/data/equipment.json` (69 entries); grep for `kCanonicalMachines` in `mobile/lib/`: 0 matches (only the deletion-note comment). |
| 11 | [RUNTIME] Client-runtime/ML telemetry -- new item, flutter-reviewer + architect review | No `firebase_performance` package anywhere (`mobile/pubspec.yaml` grepped: absent). `firebase_crashlytics: ^5.2.0` is present (`pubspec.yaml:95`) but **not imported/used** anywhere under `visual_equipment/`, `scanner/`, or `form_check/` (targeted grep, confirmed). Representative swallowed catches: `mlkit_live_equipment_service.dart:178-180`, `scanner_page.dart:179-183`, `gemini_equipment_service.dart:143-145,267-270`, `machine_describer.dart:196,231` -- all `catch (e) { debugPrint(...); ... }` with no Crashlytics forwarding. `tts_voice_coach.dart:46-52` has a `_recordError` helper whose name is misleading -- it only calls `debugPrint`/`debugPrintStack`, not Crashlytics. | **OPEN** | -- (item 6 of this gate) | `mobile/pubspec.yaml:95` (Crashlytics present, unused in these features); `mlkit_live_equipment_service.dart:178-180`; `scanner_page.dart:179-183`; `gemini_equipment_service.dart:143-145,267-270`; `machine_describer.dart:196,231`; `tts_voice_coach.dart:46-52`. |
| 12 | [CI] Secret-scanning / leaked-credential detection -- corrected to NOT remediate the existing Roboflow leak, only prevent recurrence; scope correction: extend to git history | `scripts/review/state_ledger.py:1133-1163`, `roboflow_key_not_committed()` -- a **custom, single-pattern** regex check (`_ROBOFLOW_LITERAL`, `:1133-1135`) matching only `ROBOFLOW[_A-Z]*KEY` literals. Its own docstring (`:1140-1146`) says full-history verification (`git log --all -S`) was done **once, manually**, when the row was enrolled -- ongoing/CI enforcement scans **tracked files at HEAD only** (`_tracked_text_files()` via `git ls-files`, `:1147`), fails closed to `False` if git is unavailable. No gitleaks/trufflehog/git-secrets or any general-purpose scanner found anywhere in the repo or CI config. | **PARTIAL** -- narrow single-pattern, current-tree-only enforcement exists; general-purpose + history scanning does not. | -- (item 5 of this gate, "[CI] Secret scanning including Git history") | `scripts/review/state_ledger.py:1133-1163`; grep for gitleaks/trufflehog/git-secrets: 0 hits. |
| 13 | [CI] Composed-screen visual regression coverage -- new item, flutter-reviewer review, corrected from [RUNTIME] to [CI] | `mobile/test/golden/hud_golden_test.dart` exists and is CI-enforced, covering only isolated primitive widgets: `HudPanel` (`:57`), `HudButton` (`:113`), `HudChip` (`:169`), `HudToggle` (`:212`), `HudNavBar` (`:255`). Its own doc comment (`:11-27`) states explicitly: "Deliberately NOT covered: a full-screen composition (e.g. `HomePage`)... A composed-screen golden is a reasonable follow-up." | **OPEN** | -- (item 6 of this gate, "Composed-screen visual regression") | `mobile/test/golden/hud_golden_test.dart:11-27,57,113,169,212,255`. |

## Summary

- **STALE/REPLACED (2):** items 9, 10 -- both premised on the pre-G1 architecture, both re-based
  onto their G3 equivalents above (no work carried forward that assumes direct-client Gemini calls
  or the deleted `kCanonicalMachines`).
- **PARTIAL, already fails closed but unproven (3):** items 2, 8, 12 -- the underlying mechanism
  already exists and appears sound on inspection, but none has the positive+negative proof this
  gate's DoD requires, and item 12's scope (history scanning, general-purpose patterns) is
  genuinely narrower than what OBS-1 calls for.
- **PARTIAL, incomplete coverage (2):** items 3, 5 -- real infrastructure exists but does not cover
  the full stated scope (Identity Toolkit; semantic drift vs. structural-only).
- **OPEN, genuinely absent (6):** items 1, 4, 6, 7, 11, 13.
- **No item was accepted on the master plan's own word alone** -- every row above was independently
  re-verified against current source at HEAD `1a8bf816`, per this step's own reject condition.
