# B3 — фото людей в карточках: диагноз ОПРОВЕРГНУТ (2026-08-07)

**Первая редакция этого файла (коммит `0046129`) была неверна. Ниже — исправление.**

## Что я утверждал и почему это было неправильно

Я написал, что фотографии людей приходят из полей `frames` / `imageUrls`
(Free Exercise DB) и что это живое поведение. Основанием был комментарий
`lib/features/equipment/workout_player_page.dart:157-160`.

Комментарий описывает **удалённый** код:

> The two photograph fallbacks that used to sit here **are gone**: `frames` and
> `imageUrls` are both stills of a man in a gym…

Я прочитал описание того, что убрали, как описание того, что есть. Это ровно та
ошибка, против которой написано правило «цитируй источник»: цитата была, но
смысл её я перевернул.

## Что происходит на самом деле

**Фотографий в текущем коде нет. APK у оператора старше исправления.**

Доказательство — сравнение двух каталогов:

| | до 2026-08-03 (`f9d5913:mobile/assets/data/exercises.json`) | сейчас (`exercises_vendor.json`) |
|---|---|---|
| строк | 511 | 1887 |
| корни медиа | `assets/exercises/…` **417**, `https:` **284**, `assets/…` 549 | только `assets/posters/…` |

`assets/exercises/` — это и есть «132 files, 8.1 MB of **demo photographs**»
(`mobile/pubspec.yaml:123-126`), удалённые 2026-08-04.

Исправление пришло коммитом **`88d0759` (2026-08-03)** — *«feat(H1): the catalog
shows animation or it shows nothing»*.

Проверка постера, который текущий код отдал бы в шапку для жима на плечи:
`assets/posters/men/ea_iso_lateral_shoulder_press_alternate_arms.jpg` — открыл,
это **3D-рендер анатомической фигуры на белом**. На скриншоте оператора
(`Screenshot_20260806_133411.jpg`) в том же месте — тёмное фото живого мужчины
со штангой. Текущий код такую картинку взять неоткуда.

Замер по всем постерам: 2979 файлов, 1.9–11.3 КБ, медиана 4.4 КБ. Фотографий
среди них нет — но этот вывод из размера файла был догадкой, пока я не открыл
конкретный файл. Открыл; вывод подтвердился, но порядок должен быть обратным.

## Следствие

**B3 не требует правки кода.** Требуется пересобрать и переустановить APK.

Скриншоты датированы 06.08 — на три дня позже исправления, поэтому «свежий
скриншот» и «свежая сборка» здесь разошлись. Правило про скриншоты
(`~/.claude/CLAUDE.md`, «Screenshot Timestamps») ровно об этом: картинка
заморожена в момент съёмки, а сборка — в момент установки.

**Проверять на пересобранном APK, а не на скриншотах.**

## Что это НЕ отменяет

- **Баг 1 (дорожка) остаётся.** Он измерен мной против той самой `.tflite`,
  что лежит в репозитории сейчас, а не против сборки оператора: пороги 0.05/0.10
  пропускают 30 фото из 30, модель даёт 0.892 на машине без класса. См.
  `B1_RECOGNITION_MEASUREMENT_2026-08-07.md`. Частично исправлено в `6d63e98`,
  лечение — B5.
- **Баг 3 (женские клипы мужчине) остаётся.** `playableVideoFor`
  (`equipment_models.dart:178`) в текущем коде: `video[body] ?? video['men'] ??
  video.values.first`. Оператор решил оставить как есть.
- **B6 (дебаг-логи) становится важнее, а не менее важен.** Именно он показал бы
  версию сборки в логе и снял бы этот вопрос за секунду вместо часа раскопок.

## Что делать дальше

1. Пересобрать APK с текущего HEAD, поставить на телефон, повторить сценарий с
   `Screenshot_20260806_134541.jpg`.
2. Если фото людей остались — диагноз снова открыт, и тогда искать надо в
   `_EquipmentThumb` (`equipment_detail_page.dart:207-251`) и `_ExerciseCard`
   (:253): у обоих есть градиентная заглушка, и обе ветки живые.
3. Если пропали — закрыть B3 как «исправлено ранее, подтверждено на сборке».

## Состояние гейтов

- B1 — готов (`B1_RECOGNITION_MEASUREMENT_2026-08-07.md`)
- B2 — готов, коммит `6d63e98`
- B3 — кода не требуется; ждёт пересборки для подтверждения
- B4 — оставлен как есть по решению оператора
- B5, B6 — не начаты
- Не запушено ничего после `d5bc578`
