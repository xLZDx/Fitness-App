# План: баги со скриншотов 13 августа + остаток

Статус на 2026-08-13. Авторизовано оператором: **B7 → B1, B6, B8** (одна цепочка,
один коммит), затем **B2a+B2b**, затем **B4**. Остальное — не авторизовано, ждёт GO.

Все находки ниже измерены чтением кода или прогоном на эмуляторе, не по памяти.

---

## Устройство: петля появилась

`D:\android-sdk\platform-tools\adb.exe`, `emulator-5554` (Android 14, x86_64, 320x640 @ 160dpi).

**Ключевой факт, который стоил часа:** release-сборка ставится под `applicationId`
**`com.fitnessapp.fitness_app.sptr`** (`mobile/android/app/build.gradle:105`), а на эмуляторе
жил ещё и `com.fitnessapp.fitness_app` — остаток от сборки 6 августа, до переименования.
Первые скриншоты снимались с него, то есть с чужого недельного билда. Проверять версию
надо через `aapt2 dump badging` на самом APK, а не через `dumpsys` установленного пакета.

versionCode у split-per-abi сборок различается по ABI: arm64 = 2367 (то, что у оператора),
x86_64 = 4367 — **это один и тот же билд**, а не два разных.

Анонимный вход («Продолжить») на эмуляторе работает, онбординг проходится. Значит петля
«поставить → пройти → снять экран» доступна целиком, и давний пробел «ни один экран серии
R11/O не открывался на устройстве» закрывается не словами.

---

## Блок B — баги со скриншотов

### B7 · Звук и AI-комментарии не выключаются — СДЕЛАНО 13.08, коммит `09ef865`

**Дефект, доказанный из кода.** `setVoiceEnabledProvider` читается ровно один раз, в
`start()` (`set_timer_providers.dart:140`). Ни одна строка на этом пути не вызывает `stop()`
у голосового тренера. Form Coach делает это правильно: `if (isMuted) unawaited(coach.stop())`
(`form_check_providers.dart:573`).

Следствие: нажатие «выключить голос» во время речи не прерывает её — тренер договаривает.
С точки зрения пользователя кнопка не реагирует. То же с гонгами: `_play()`
(`:177-185`) гасит только новые звуки.

**Почему тест не поймал:** `'voice off means nothing is said'`
(`test/features/workouts/set_timer_controller_test.dart:182-192`) выключает голос **до**
старта. Дыра ровно в переключении **после**.

Правка: слушать оба провайдера в контроллере и останавливать воспроизведение немедленно.
Тест: включить голос, стартовать, выключить — ожидать вызов `stop()`.

### B1 · Фигура тела на вкладке «Приоритеты» — СДЕЛАНО 13.08, коммит `09ef865`, подтверждено на эмуляторе (`e4366b5`)

Подтверждено на устройстве (`D:\tmp\before_priorities.png`): «Ограничения» показывают фигуру,
«Приоритеты» — только чипы (`step_body.dart:120-137`).

`BodyZoneMap` жёстко привязан к `InjuryRegion` (`body_zone_map.dart:21,54,147`). Обобщить до
`Map<T, List<Rect>>`, добавить карту для `FocusZone`. `fullBody` фигурой не выражается —
остаётся только чипом; это не упущение, а следствие того, что «всё тело» не зона.

### B6 · Управление скоростью — КОД СДЕЛАН 13.08, коммит `09ef865`; на устройстве не проверен

`exercise_reference.dart:561-580`: `Positioned(right: 8, bottom: 8)`, горизонтальный `Row`
из `ChoiceChip` поверх видео. Перенести влево, вертикально, компактнее.

### B8 · Вес — СДЕЛАНО 13.08, коммит `09ef865`, подтверждено на эмуляторе (`e4366b5`)

Сейчас `SetCaptureSheet.show` безусловный (`workout_player_page.dart:263`), сразу за ним
второе окно `DifficultyRatingSheet.show` (`:359`).

**Решение оператора (2026-08-13):** вес спрашивать **в начале** упражнения, при старте, и
**только если упражнение вообще подразумевает вес**. Йога, присед, отжимания, скручивания —
не спрашивать вообще.

Признак нагрузки берётся из `ExerciseItem`: `equipmentId` / `equipmentLabel` / `isStretch`
(`equipment_models.dart:52,58,72`).

### B2a+B2b · Английский по умолчанию — СДЕЛАНО 13.08, коммит `eb66c7e`

- **B2a** — шесть заголовков программ заданы русскими литералами
  (`programme_templates.dart:54,62,71,79,88,95`), l10n нет вообще. Это то, что видно на
  скриншоте «Train / Силовая база».
- **B2b** — названия упражнений сохраняются **снимком** на момент записи
  (`programme_schedule.dart:78`, `workout_player_page.dart:466`), поэтому «Recent activity»
  смешан. Резолвить по `exerciseId` из каталога, снимок оставить фолбэком.

Сам каталог **не виноват**: база английская, `.ru.json` — оверлей, и `_loadOverlay`
возвращает пусто для `en` (`asset_equipment_repository.dart:77`). Правило «сначала
английский» там уже соблюдено.

### B4 · Человеческий силуэт — КОД СДЕЛАН 13.08, коммиты `dd644d1` + `e1714e1`; ЖДЁТ ВЗГЛЯДА ОПЕРАТОРА

`_SilhouettePainter` (`form_check_page.dart:1391-1413`) рисует независимые `drawLine` плюс
окружность головы — это утолщённые палочки, а не силуэт. Нужен замкнутый контур с переменной
шириной. Менять `pose_silhouette.dart` (`buildSilhouette`), а не только рисовалку.

Третий заход на эту картинку. Закрытым считаю только после взгляда оператора.

### B3 · Описания техники — СДЕЛАНО 13-14.08

Измерено: **403 из 1887 (21%)** без шагов, одинаково в `exercises_vendor.json` и в
`.ru.json`. Это дыра в **данных**. Порядок: проверить `1500+ exercise data.xlsx` на наличие
инструкций для этих id → доработать `scripts/catalog/` → перегенерировать. Если в источнике
их нет — это решение о контенте, и работа останавливается со списком.

**Ход 13-14.08 (GO оператора: писать текст самому).** Источник проверен: у всех 403 ЕСТЬ клип,
пусты ровно два текстовых поля, и это одно и то же множество строк. Нехватка не техническая.
Обнаружено второе: `summary` обязан быть копией первого шага (инвариант каталога, есть тест),
поэтому поля под «зачем это нужно» не существовало — заведено `purpose`. Инструмент слияния:
`tools/catalog/merge_exercise_text.py`, батчи в `tools/catalog/batches/`. Полный список:
`core/plans/B3_EXERCISES_WITHOUT_STEPS_2026-08-13.csv`.

**Написано 403 из 403 — строк без шагов больше нет.** Двенадцать партий по группам: предплечья и
пауэрлифтинг 10, бицепс и трицепс 26, грудь 19, спина 28, плечи 46, ноги 70, пресс 33, растяжка 41,
йога 42, калистеника и функционал 88. У каждой — назначение и пять шагов на двух языках.

**Что осталось.** (1) У 1484 строк, пришедших от поставщика с шагами, `purpose` нет — это новый
разрыв, созданный самим полем: у 403 «Зачем это нужно» есть, у 1484 нет. (2) Текст написан
ассистентом, не тренером; для 403 записей это стоит показать человеку с профильной квалификацией.
(3) `purpose` виден только на карточке упражнения — ни карточка программы, ни плеер его не
показывают. Подробности и найденный на гейте BLOCKER (русский текст не доходил до экрана) — в
`core/DECISION_LOG.md`, записи 01:52 и 03:10.

### B5 · Программы — НЕ АВТОРИЗОВАН, разбит на четыре

Сработал порог масштаба (>2ч / 15+ файлов), поэтому подгейты.

Что не так, по коду:
- `buildProgrammeSchedule` принимает только `programme` + `catalogue`
  (`programme_schedule.dart:39-42`) — анкета не читается **никак**: ни оборудование, ни
  место, ни цель, ни уровень, ни `focusZones`, ни `daysPerWeek`;
- `catalogue` = `safeCatalogProvider`, то есть единственный фильтр — травмы;
- **одно упражнение на день** (`:71-82`) — отсюда «одно упражнение на программу»;
- шесть фиксированных шаблонов, ничего не рассчитывается;
- в карточке нет картинок упражнений.

- **B5a — СДЕЛАНО 13.08.** Каталог фильтруется по месту и оборудованию **до** генератора
  (`availableWith`), `focusZones` наполняют полнотельные шаблоны, `daysPerWeek` зажат тем,
  что человек указал. P4 закрыт: `FocusZone` теперь читается. Отклонения от строки выше,
  оба измерены, а не выбраны: **учёта уровня не будет** — `difficulty` равен `beginner` у
  всех 1887 упражнений, фильтр не смог бы сработать (градация каталога — отдельная задача
  по контенту, рядом с B3); **цель** за пределами выбора шаблона не используется — вывести
  из неё методику значило бы её выдумать, это работа B5d. Подробности и пойманный агентом
  BLOCKER — в `core/DECISION_LOG.md`, запись 21:58.
- **B5b — СДЕЛАНО 13.08, кроме перехода в плеер.** Разбит на четыре подгейта по порогу масштаба
  (24 файла в `lib/` + 20 в `test/` читают `ScheduledSession`), `GO B5b-1..4`. Сделано:
  форма данных с фолбэком на старое поле (без миграции), наполнение дня до указанной длительности
  занятия, скрининг травм и офлайн-предзагрузка по всему дню, счётчик «+N» на главной и в
  расписании, дайджест дня считает упражнения, а не строки. **Не сделано:** нажатие на день
  открывает только первое упражнение — плеер устроен вокруг «входного упражнения», и завести в
  него готовый день это отдельная работа. Подробности — в `core/DECISION_LOG.md`, запись 22:54.
- **B5b-плеер — СДЕЛАНО 14.08.** Тот самый «остаток». День открывается как одна тренировка:
  маршрут `/workout/:id` читает `?day=`, плеер получает `dayId`, внутри дня сессия ключуется днём,
  а не входным упражнением, и упражнения сливаются по `exerciseId` (`upsertExerciseById`), а не по
  позиции. Полоса дня в плеере (решение оператора: не отдельный экран) показывает «упражнение 2 из
  4», зачёркивает сделанные и переключает между ними. Идентификатор строки истории выведен из дня
  (`daySessionId`), а не из часов, иначе `autoDispose` + `replace` маршрута открывали второй ряд на
  тот же день. Побочно найден и починен реальный оверфлоу кнопки «добавить упражнение» на 400px.
- **B5c — СДЕЛАНО 14.08.** Карточка текущей программы показывает ближайший день плитками упражнений
  (`_ProgrammeDayThumbs`), число плиток меряется по ширине, остаток называется числом. Противопоказанное
  упражнение сохраняет слот, но теряет картинку. **Отклонение от строки выше, измеренное:** карточка
  **шаблона** картинок не получила — `ProgrammeTemplate` не несёт ни одного упражнения
  (`programme_templates.dart:33-53`), нарисовать «что внутри» до запуска генератора можно только выдумав
  это; предпосылка появится в B5d. Подробности — в `core/DECISION_LOG.md`, запись 09:57.
- **B5d** — программа генерируется из анкеты; шаблоны остаются рекомендациями. Разбит на три подгейта
  по порогу масштаба.
  - **B5d-1 — СДЕЛАНО 14.08.** Расписание встаёт на дни недели, которые человек отметил в анкете
    (`preferredWeekdays` не читал никто). При противоречии с «сколько дней в неделю» побеждают
    отмеченные дни, `Programme.daysPerWeek` следует за реально запланированным числом. Попутно
    починена DST-арифметика дат: `add(Duration(days:))` съезжал на соседний день недели. Подробности —
    в `core/DECISION_LOG.md`, запись 10:10.
  - **B5d-2 — СДЕЛАНО 14.08.** На вкладке «Программы» появилась карточка «Собрать по моим ответам»:
    цель, уровень, дни и зоны берутся из анкеты, шаблон выбирать не нужно. Собранная программа идёт
    через тот же `enroll`, что и шаблонная — отдельного пути записи нет. Карточка скрыта, если анкета
    пуста (обещание «по твоим ответам» без ответов было бы неправдой). Подробности — в
    `core/DECISION_LOG.md`, запись 14.08 10:24.
    - **Названо, а не выведено:** длительность программы. Анкета не спрашивает, сколько недель
      человек хочет, поэтому это константа `kProfileProgrammeWeeks = 8` (длина двух из шести
      шаблонов, середина их диапазона 4..10). Вывести её из цели — «12 для массы, 4 для возвращения» —
      значило бы выдумать методику и выдать это за персонализацию.
  - **B5d-3 — СДЕЛАНО 14.08.** Шесть шаблонов на вкладке «Программы» теперь идут по соответствию
    анкете: у лучшего значок «Лучше всего подходит», под каждым — строка «Подходит по цели,
    уровню…». Соответствие — четыре независимых да/нет (цель, уровень, расписание, зоны), а не
    процент: четыре булева не дают точности, которую подразумевает «87% совпадения», и названные
    измерения человек может сверить со своими же ответами. Подробности — в `core/DECISION_LOG.md`,
    запись 14.08 10:46.
    - **Неотвеченный вопрос никогда не считается совпадением**, ранжирование при пустой анкете не
      применяется вовсе, а значок не ставится при ничьей — приложению в этом случае не на чем
      назвать один шаблон лучшим.
  - **Измеренное, не выбранное (обновлено 14.08):** после B5d-2 `goals.primary` и `level.tier`
    доходят до `Programme.goal`/`Programme.level` на пути «из анкеты». Поведения за этими полями
    по-прежнему нет — их читают только подпись карточки и фильтр по цели, ни один генератор на них
    не смотрит. Для шаблонной программы они как были копией шаблона, так и остались.

---

## Блок R — остаток редизайна (не авторизован)

- ~~**R11h**~~ — **сделано, моя ошибка в первой версии этого файла.** Я перенёс пункт из
  `PLAN_REDESIGN_REMAINDER_2026-08-12.md`, не проверив его. Проверено 13.08:
  `coachPhaseControllerProvider` имеет восемь вызывающих в `lib/`, `form_check_page.dart:240-241`
  рисует `CoachLaunchCard` и экран подготовки, а камера открывается по кнопке
  (`:145 openCamera()`), а не при входе. Тест `coach_intro_cards_test.dart` — «the camera opens
  on the preparation card's button, and only then» — это пиннит. Плановый файл от 12 августа
  устарел и по R11b, и по R11h.
- **R11f-1 · privacy gate — СДЕЛАНО 14.08, коммит `b9b8e05`, отправлено.** Построено как
  СОГЛАСИЕ по решению оператора 13.08: камера не открывается, пока ответ не «да». Гейт стоит
  ПЕРЕД шторкой съёмки, ответ привязан к аккаунту, отказ чтения означает «спросить снова».
  Найдено и исправлено при разборе: текст согласия обещал удаление фото, которого в приложении
  нет — фраза убрана, отсутствие удаления названо (см. H6 ниже). Подробности — в
  `core/DECISION_LOG.md`, запись 14.08 18:15.
- **R11f-2 · export — ЗАКРЫТО WONT_BUILD, 2026-09-16, GPT-PM VERDICT: APPROVE.** Экспорт
  фото прогресса формально не строится: `ExportScreen` из прототипа (`App.tsx:3931`)
  окончательно считается superseded, а не «решение не принято». Обоснование то же, что уже
  было в коде (`local_progress_photos_repository.dart:64-72`) и в решении R7 от 08.08
  (`core/SESSION_STATE_2026-08-08.md:375-377`) — расшифрованный JPEG в системном шторке «поделиться»
  разрушает обещание «фото не покидают телефон»; GPT-PM подтвердил это как продуктовое решение, а
  не как техническую блокировку, и явно оставил дверь открытой на будущее ("if actual user demand
  later justifies option B, reopen it as a new privacy/product gate" — новый гейт с явным
  консент-шагом на один файл, а не старый `ExportScreen` без изменений). Подробности —
  `core/DECISION_LOG.md`, запись «Tier C rows 12/19».
- **R11f-3 · напоминание** — область планировщика уведомлений.
- **Ф3** — поэкранная сверка с прототипом, честно после R11h/R11f.

**R11b считается закрытым** серией O от 12 августа; файл `PLAN_REDESIGN_REMAINDER_2026-08-12.md`
на этот счёт устарел и перечисляет как недостающие виджеты, которые уже есть.

---

## Блок P — платформенные хвосты (не авторизован)

- **P1** — `compileSdk 35→36`. Предупреждение на каждой сборке, со временем блокер Play.
- **P2** — debug и release делят один `applicationId` (`build.gradle:133`). Установка
  отладочной сборки стирает данные релизной и ломает пару с Wear. **Требует отдельного GO
  на подписи.** Уточнение 13.08: телефон и Wear **совпадают** по id (`...sptr`,
  `wear/build.gradle.kts:20`) — расходятся именно debug и release.
- **P3 — СДЕЛАНО 14.08, коммит `bbcabc7`.** 207 упражнений получили тег `upper_back`
  (11.0% каталога, между `wrist` 188 и `ankle` 229). Причина нуля была не в данных: в `RULES`
  теггера не было ключа для этой зоны, а единственный страж проверял обратное направление —
  закрыто тестом `test_every_legal_region_has_rules`. Два собственных дефекта пойманы прогоном:
  токен ловил подтягивание, скопированный muscle-gate ронял 9 из 25 шрагов. Подробности — в
  `core/DECISION_LOG.md`, запись 14.08 18:28.
- **P4** — закрывается внутри B5a.

---

## Блок A — остаток аудита (не авторизован)

- **A1** — маркировка сканера / Form Coach / осанки как экспериментальных. Единственный
  пункт, который `ML_STRATEGY_2026-08-11.md` называет обязательным до релиза.
- **A2** — пустое `notificationsRule` в бюджете `projects/988522745882`.
- **A3** — P1g, порядок съёмки фото: нужен телефон.
- **A4** — версия API живого Stripe: доступ к `STRIPE_SECRET_KEY` запрещён классификатором
  среды, прочитать нельзя.
- **A5** — Paywall, HELD на ценообразовании.

---

## Блок H — хвосты, названные при закрытии гейтов (не авторизованы)

Их нет в исходном списке багов: это то, что я сам назвал недоделанным, закрывая B5b/B5c/B5d и B3.
Записаны здесь, чтобы не жить только в `DECISION_LOG.md` и в чате.

- **H1 — СДЕЛАНО 14.08, коммит `8f26797`.** ~~заголовок дня в истории~~ — **формулировка выше
  была неверна и это проверено по коду.** `WorkoutSession.title` действительно ставится в имя
  первого упражнения (`workout_player_page.dart:582`), но его не читает ни один виджет и мимо
  него идёт даже выгрузка GDPR. Настоящий дефект: «Recent activity» показывала строку НА
  УПРАЖНЕНИЕ, поэтому день из четырёх читался как четыре отдельные тренировки и занимал весь
  список из пяти карточек. Теперь одна карточка на тренировку, название «первое упражнение +N».
  Подробности — в `core/DECISION_LOG.md`, запись 14.08 18:27.
- **H6 · удаления фото в приложении нет** — найдено при разборе R11f-1.
  `ProgressPhotosController.delete` существует (`progress_photos_providers.dart:338`) и не
  вызывается ни одним виджетом; `_PhotoTile` не имеет ни `onTap`, ни `onLongPress`; ключ
  `progressphotosDelete` лежит в обеих ARB без читателя. Не регрессия — так было и до гейта.
- **H2 · ранжирование не смотрит на оборудование и травмы** — на подбор упражнений ВНУТРИ
  программы они влияют (`availableWith` + отбор по травмам в `enroll`), на порядок карточек нет.
  Шаблон не перечисляет требуемое оборудование, вывести можно только прогнав генератор для
  каждого из шести. Остаток B5d-3.
- **H3 · 1484 из 1887 строк каталога без `purpose`** — измерено 14.08 по
  `assets/data/exercises_vendor.json`. Для сравнения: без `steps` не осталось ни одной, это
  закрыл B3. Поле `purpose` появилось в том же гейте, наполнено только у 403 строк, которые B3
  трогал.
- **H4 · `semanticLabel` на общем `ExerciseThumb`** — плитки упражнений в карточке программы
  для скринридера безымянны. Остаток B5c.
- **H5 · 89 строк каталога с противоречивой группой мышц** — замер B3, не чинилось.

---

## Порядок

**Сделано и отправлено** (для истории): B7 → B1, B6, B8 одной цепочкой (`09ef865`);
B2a+B2b (`eb66c7e`); B4 (`dd644d1`, `e1714e1`, ждёт взгляда оператора); B3
(`6bc4203` → `fb64615` → `a2d899c`); B5a (`8d44fa8`); B5b (`ec088fc`, `27ef038`);
B5c (`891a86a`); B5d-1/2/3 (`52c5b76`, `0234236`, `760a361`). Всё на `origin/master`.

**Сделано 14.08 и НЕ отправлено** (нужен push-GO): H1 (`8f26797`), P3 (`bbcabc7`).
**Сделано 14.08 и отправлено:** A1 (`9672960`), P1 (`d586a31`), R11f-1 (`b9b8e05`).

**Дальше — только по новому GO.** Порядок ниже — исходный; пункты 1-3 и 4-5 закрыты, оставлены
для истории вместе с основанием, по которому их так упорядочили:

1. ~~**A1 · маркировка экспериментального**~~ — единственный пункт, который `ML_STRATEGY_2026-08-11.md`
   называет обязательным ДО релиза. Дёшев, и снимает главный риск: обещать пользователю то,
   чего система не делает.
2. ~~**P1 · `compileSdk 35→36`**~~ — не теория: сборка 14.08 напечатала требование от
   `flutter_secure_storage` и `flutter_tts`. Механическая правка, со временем блокер Play.
3. ~~**R11f-1 · privacy gate как согласие**~~ — решение оператора уже записано (13.08), работа
   определена. Чем дольше лежит, тем дороже возвращаться в контекст.
4. ~~**H1 · заголовок дня**~~ — видно пользователю в истории, правка мелкая.
5. ~~**P3 · `upperBack`**~~ — зона без единого упражнения, сейчас это тупик в UI: либо наполнить,
   либо убрать.
6. **P2 · `applicationId` debug/release** — только отдельным гейтом: цена ошибки это стёртые
   данные на телефоне оператора и сломанная пара с Wear. **Требует GO на подписи.**
7. **H3 · 1484 `purpose`** — отдельный длинный прогон, как B3. Между другими гейтами не
   встраивается.
8. **H2 · оборудование в ранжировании** — требует прогонять генератор для каждого шаблона,
   заметная работа.
9. **Ф3 · поэкранная сверка** — честно последней, после R11f.

**Заблокировано не выбором, а средой:** A3 (нужен телефон), A4 (доступ к `STRIPE_SECRET_KEY`
запрещён классификатором среды), A5 (HELD на ценообразовании), R11f-2 (конфликтует с записанным
решением «фото не покидают телефон», решения нет).

**Мелочь без места в порядке:** H4 (`semanticLabel`), H5 (89 строк с противоречивой группой) —
подобрать попутно, когда гейт и так трогает эти файлы.
