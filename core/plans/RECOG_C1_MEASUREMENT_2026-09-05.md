# RECOG-C1 — the measurement

Generated by `test/tools/recog_c1_compute_metrics.dart`, which was committed before the first observation existed. Every scoring decision below comes from the frozen `recog_c1_contract.dart`; this file contributes the join, the denominators and the arithmetic only.

## Counts

| | |
| --- | --- |
| observations recorded | 104 |
| planned | 104 |
| reached the model | 104 |
| operational failures | 0 |
| scored (semantic) | 104 |
| scored and resolved | 98 |
| control records | 104 |

## Control records

104 write-ahead `attempt_started` markers, 0 of them with no matching observation.

No stop, abort or resume record — every stop condition would have left one.

## Response classes, per arm

| class | arm A | arm B |
| --- | --- | --- |
| `canonicalResponse` | 52 | 52 |

## Semantic scores over resolved rows, per arm

| score | arm A | arm B |
| --- | --- | --- |
| `correctConfident` | 18 | 22 |
| `overclaimed` | 25 | 25 |
| `safeNonOverclaim` | 1 | 1 |
| `wrongConfident` | 5 | 1 |

## The crop effect

Comparable pairs: **49**. Not comparable: **3** — each named below, because a pair dropped without a reason is a pair nobody can check.

| | arm A (full frame) | arm B (viewfinder crop) |
| --- | --- | --- |
| correct and confident | 18 | 22 |

| pair | why it is not comparable |
| --- | --- |
| `p15` | neither arm entered a correctness number: ground truth unresolved, or the attempt never reached the model |
| `p29` | neither arm entered a correctness number: ground truth unresolved, or the attempt never reached the model |
| `p47` | neither arm entered a correctness number: ground truth unresolved, or the attempt never reached the model |

## What these numbers cannot tell you

- The corpus holds **0** out-of-catalogue single machines and **0** frames with no equipment, so it cannot measure whether the model overclaims on a machine it was never told about, nor whether it abstains honestly on an empty frame. Two of the most important properties are simply not covered.
- Three photographs are frozen `unresolved` and enter no correctness number in either direction.
- Both arms were labelled by one person in one sitting, each from its own images. Per-arm structure makes a genuine divergence visible; it does not make the judgement independent.
