# OBS-1 reconciliation — MVP1.G3, Step 10C

Base for "current implementation": HEAD `83636be9172c0692b12994d7b49e1400f30a46de` +
wording fix `381ed54` (2026-08-28), after Step 9A/9B (canary), Step 10A (enforcement
dashboard), Step 10B (device telemetry), and this session's alert-filter remediation.
Supersedes `core/OBS1_G3_REBASELINE_2026-08-27.md` for disposition purposes; that file's
own per-item evidence trail is still the record of what changed *since* the 2026-08-26
master plan and is cited below rather than repeated.

Per GPT-PM's own binding rule from Step 10C kickoff: **every row ends CLOSED or
REBASED→CLOSED — no PARTIAL/PROVISIONAL hiding in prose.** (An earlier draft of this
document loosened that to "a visible, disclosed PARTIAL is acceptable" — GPT-PM's
round-1 adversarial review of this table caught that as a real defect: a table cannot
silently redefine its own exit criterion. See `core/DECISION_LOG.md`, "GPT-PM adversarial
closure review round 1.") Live/CI proof is listed separately from implementation
evidence so a reader can see what was actually run/observed, not just what was written.

| # | Original OBS-1 item (2026-08-26 master plan §8) | Current implementation @ HEAD | Live/CI proof | Disposition | Rebaseline rationale / replacement | Evidence |
|---|---|---|---|---|---|---|
| 1 | [RUNTIME] Auth/data-flow canary | `runProductionCanary` scheduled Cloud Function, built Step 9A, activated in production Step 9B; alert policy filter fixed this session (`messageEquals`→`eventEquals`, the row-21 defect) | Step 9B production activation; this session's live redeploy + policy patch + readback + induced-failure proof (`gcloud logging read` match confirmed, `test_marker: step10c_canary_proof_2026_08_28`) | **CLOSED** | None — built as originally scoped, plus the alert-matching defect this step found and fixed | `functions/src/canary_schedule.ts`; Step 9A/9B entries, `core/DECISION_LOG.md`; this session's "GPT-PM round 1/2" entries |
| 2 | [CI] Functions test-health enforcement | `.github/workflows/functions.yml:68,72` fails closed on `npm ci`/`npm test`, no `\|\| true`/`continue-on-error` anywhere in the file | CI-enforced on every push; positive+negative fixture proof recorded at build time | **CLOSED** | None | `core/DECISION_LOG.md:27124` ("G3 item 1 [CI]: Functions test-health enforcement — DONE") |
| 3 | [RUNTIME] Enforcement-status dashboard (App Check / Identity Toolkit) | `runEnforcementStateCheck` scheduled function, `eventEquals`-based alert filter, container-level fail-closed validation | Step 10A's 10-round arc: 2 live production deploys, 2 temporary proof-only deploy/invoke/capture/delete cycles producing genuine (not synthetic) real-failure evidence, 2 live alert-policy PATCHes with SHA-256 source==live verification, real-function redeploy confirmed by revision-name change (`-00001-caj`→`-00002-hit`) | **CLOSED** | None — the strongest-verified item of the 13 | `core/DECISION_LOG.md:30465-30536` (Step 10A round 1–10 summary and closure) |
| 4 | [RUNTIME] Billing integrity monitor (Stripe) | `reconcileDuplicateSubscriptions` inline in `stripeWebhook`; alert filter fixed this session the same way as #1 | This session's live redeploy + policy patch + readback + induced-failure proof (`test_marker: step10c_stripe_proof_2026_08_28`) | **CLOSED** | None — the row-21 defect this step exists to close; GPT-PM round 2 confirmed sufficiency, with the caveat that Cloud Monitoring incident-creation/notification delivery is proven one layer short (Logging-filter match, not confirmed incident/notification) | `functions/src/index.ts:271,1862-1917`; this session's "GPT-PM round 1/2" `core/DECISION_LOG.md` entries |
| 5 | [CI] RU/EN semantic-drift spot-check | Rescoped 2026-08-27 from full content-parity to structural parity (id coverage, step-count match, Cyrillic presence, pinned-row regression) + an explicit acknowledgment that meaning-level drift is not caught by anything today | CI-enforced (`exercise_translations_test.dart`, `catalog_description_coverage_test.dart`, `catalog_corrections_test.dart`, all in `flutter.yml`) | **REBASED→CLOSED** | Original item asked for semantic-drift detection; GPT-PM accepted the narrower structural-parity scope as the practical substitute (full semantic detection needs a translation-quality model, out of scope for this gate) — the residual semantic-drift gap is a disclosed, accepted limitation, not silently dropped | `core/DECISION_LOG.md:27389` ("MVP1.G3 OBS-1 item 7 [CI]: RU/EN translation semantic-drift detection") |
| 6 | [CI] Mobile/Dart dependency (SCA) scanning | New CI step added for `mobile/` (Dart-side equivalent of the existing backend `npm audit`) | CI-enforced in `flutter.yml` | **CLOSED** | None | `core/DECISION_LOG.md:27151` ("G3 item 2 [CI]: Mobile/Dart dependency (SCA) scanning — DONE") |
| 7 | [RUNTIME] Data-lifecycle sweep verification | **G3-CI-8 "Data Lifecycle Coverage Drift Guard"** (`scripts/ci/check_data_lifecycle_coverage.js`), built 2026-08-27 per GPT-PM's own round-2 design ruling: discovers every Firestore collection actually referenced from two independent static sources (`firestore.rules` match blocks + literal `.collection()` calls in production source, unioned), requires an explicit `DELETE`/`EXPORT`/`BOTH`/`EXEMPT` classification with a reason for each; found and fixed 2 real coverage gaps while being built (`equipment_setup_notes`, `receipts` — both deletable but not exportable) plus one unrelated rules gap (`coach_listings`, no rules entry at all) | Positive: `node scripts/ci/check_data_lifecycle_coverage.js` — 34/34 collections classified, exit 0. Negative: 3 real (not simulated-in-isolation) fixture scenarios, all fail as required. `npx jest` 382/382, rules-emulator suite 108/108, `tsc` clean | **REBASED→CLOSED** | Original item's own citations (`index.ts:1989-1992`, `account_export.ts:63,93`) described the PRE-G3-CI-8 state (literal hardcoded lists); G3-CI-8 replaced that mechanism entirely, per GPT-PM's explicit round-2 requirement that this NOT be scoped down to roadmap. GPT-PM's approved design is static-source discovery (rules + code), not live Firestore introspection (`listCollections` — zero uses anywhere, re-confirmed this session) — this is the requirement as GPT-PM actually specified it, not an accepted shortfall against it. **One residual, disclosed edge case** genuinely outside even the approved design: a collection with zero current rules/code reference but live data (fully orphaned after a feature removal) would not be caught — already once found live in a related blind spot (`_canary`, `core/DECISION_LOG.md:27996-28000`, fixed by re-running the check after the gap was noticed, not by the check catching it unprompted) | `scripts/ci/check_data_lifecycle_coverage.js`; `core/DECISION_LOG.md:27615-27738` (round-2 ruling + G3-CI-8 build), `27996-28000` (the `_canary` blind-spot instance); this session's re-confirmation, `core/DECISION_LOG.md:31334-31341` |
| 8 | [CI] Safety-invariant regression guard | `SafetyContext` is `required` at `plan_builder.dart:33` and `programme_builder.dart:274,288`; explicit regression test added naming this invariant directly | CI-enforced | **CLOSED** | None | `core/DECISION_LOG.md:27182` ("G3 item 3 [CI]: SafetyContext invariant regression guard — DONE") |
| 9 | [RUNTIME] Gemini/Firebase AI Logic client-direct call monitoring | Client-direct-call half: G1 (closed 2026-08-26) migrated all 4 AI call sites off direct client calls, routed through `httpsCallable`. Observability half: 5 log-based metrics defined for the 4 AI Gateway callables (`aiCoachAdvice`, `aiEquipmentRecognition`, `aiMachineDescription`, `aiExerciseGeneration`) | Client-direct-call half: re-grepped this session, zero `firebase_ai`/`GenerativeModel(`/`.generateContent(` matches in `mobile/lib/`. Observability half: metrics exist in source but the 4 callables are **not deployed** — re-confirmed live via `gcloud functions list` (16 functions, zero AI callables) | **Split: client-direct-call half CLOSED; observability half PARTIAL** | Original item was premised on the pre-G1 architecture (direct client calls) and no longer applies as written — the direct-call risk is closed. The replacement risk (undeployed observability for the new server-side callables) is real and open: metrics with zero possible data points cannot be called "observed" in the sense OBS-1 asked for. **Tail**: deploy the 4 AI Gateway callables and re-verify the 5 log metrics receive real data, or explicitly descope AI Gateway callable deployment to a later gate and record that as a deliberate decision | `mobile/lib/features/equipment/gemini_equipment_service.dart:23,67-75`; `core/G3_STEP8_RUNTIME_PREREQUISITES.md` §6.6; `core/DECISION_LOG.md:31342-31348`; `gcloud functions list` (this session) |
| 10 | [CI] `kCanonicalMachines` ↔ `equipment.json` parity check | Content-level (id/name) parity check between `functions/src/ai_equipment_recognition.ts` `CANONICAL_MACHINES` (69 entries) and `mobile/assets/data/equipment.json` (69 entries) | CI-enforced; a real pre-existing drift was found and fixed by this check when it was built | **CLOSED** | Original item's premise (`kCanonicalMachines` in `mobile/lib/`) no longer exists (deleted, replaced by the server-side list) — the duplication moved, the parity check follows it | `core/DECISION_LOG.md:27212` ("G3 item 4 [CI]: equipment-registry parity — DONE, and it found a real pre-existing drift") |
| 11 | [RUNTIME] Client-runtime/ML telemetry | Step 10B wired `mlkit_live_equipment_service.dart`, `scanner_page.dart`, and `gemini_equipment_service.dart` (2 call sites) to real `FirebaseCrashlytics.recordError`, with genuine device-to-backend proof (S8 test device). This session wired the 2 remaining files GPT-PM's round-1 closure review flagged: `machine_describer.dart` (both catches, now capturing `stackTrace` too) and `tts_voice_coach.dart` (`_recordError`'s single centralized helper, gated by a new `expected` flag so "no TTS voice installed" stays a state, not a false alert) | Step 10B's device-to-backend E2E proof (round 4/5) for the original 3 files; this session's `flutter analyze` (clean) + `flutter test test/features/visual_equipment/ test/features/form_check/` (628/628 pass) for the 2 new files, plus 4 new unit tests (`tts_voice_coach_test.dart`) proving the Crashlytics wiring doesn't crash the caller — `machine_describer_test.dart`'s existing 31 tests already proved the same for its own file | **CLOSED** | None — completed rather than rebaselined, since this was the same mechanical pattern Step 10B already proved for 3 other files of the same kind | `mobile/lib/features/visual_equipment/data/machine_describer.dart:196-213,231-249`; `mobile/lib/features/form_check/data/tts_voice_coach.dart:46-71`; `mobile/test/features/form_check/tts_voice_coach_test.dart`; `core/DECISION_LOG.md`, "#11 (client-runtime/ML telemetry) genuinely closed" |
| 12 | [CI] Secret-scanning / leaked-credential detection (incl. Git history) | Extended beyond the original single-pattern Roboflow-key check to general-purpose + history scanning | CI-enforced; a real live secret was found by this check when it was built | **CLOSED** | Original item was corrected 2026-08-26 to explicitly NOT remediate the pre-existing Roboflow leak (operator/console action, not code-fixable) — scanning prevents recurrence only. That correction still holds; it is a scope note, not an open gap in this check itself | `core/DECISION_LOG.md:27265` ("G3 item 5 [CI]: secret scanning including Git history — DONE, and it found a real live secret") |
| 13 | [CI] Composed-screen visual regression coverage | Golden test added covering a full composed screen (previously only isolated primitives: `HudPanel`, `HudButton`, `HudChip`, `HudToggle`, `HudNavBar`) | CI-enforced | **CLOSED** | None | `core/DECISION_LOG.md:27324` ("MVP1.G3 OBS-1 item 6 [CI]: composed-screen visual regression") |

## Summary

- **CLOSED (10):** #1, #2, #3, #4, #6, #8, #10, #11, #12, #13.
- **REBASED→CLOSED, gap disclosed (2):** #5 (semantic-drift narrowed to structural parity,
  accepted), #7 (data-lifecycle check is static-source-only, not live-Firestore, accepted
  as the practical substitute — orphaned-live-collection detection remains genuinely
  absent).
- **Still open, not yet a valid Step 10C exit state (1):** #9 (AI Gateway observability
  half — 4 callables undeployed, metrics unproven). Per GPT-PM's own binding rule
  ("every row ends CLOSED or REBASED→CLOSED, no PARTIAL/PROVISIONAL hiding in prose"),
  this row does not yet count as closed under any label; a plan to resolve it (deploy and
  prove, or an explicit rebaseline) is in flight, see `core/DECISION_LOG.md`.
- Every REBASED row carries an explicit disclosed gap rather than a silent scope change;
  #9 is the one row Step 10C has not yet closed.
- No row's disposition rests on the master plan's or the 2026-08-27 rebaseline's word
  alone — every row above was independently re-verified against current HEAD and, where
  applicable, live production state, this session.
