{
  "name": "functions-equipment-identity",
  "description": "The equipment-identity Cloud Functions codebase, isolated from the default (Stripe) codebase per P0.G6 -- see core/equipment_identity/p0/P0_G6_DEPLOYMENT_ISOLATION.md. No production identity function is exported yet -- see core/equipment_identity/p0/P0_G5_CLOUD_FEASIBILITY.md for why.",
  "scripts": {
    "build": "npm run check:p0-snapshot && npm run check:p1-generated && tsc",
    "build:watch": "tsc --watch",
    "deploy": "firebase deploy --only functions:equipment-identity",
    "pretest": "npm run check:p0-snapshot && npm run check:p1-generated",
    "test": "jest",
    "sync:p0-snapshot": "node scripts/sync_p0_type_snapshot.js",
    "check:p0-snapshot": "node scripts/sync_p0_type_snapshot.js --check",
    "sync:p1-generated": "node scripts/sync_p1_generated.js",
    "check:p1-generated": "node scripts/sync_p1_generated.js --check",
    "generate:p1-g2-candidates": "npm run build && node scripts/generate_p1_g2_artifacts.js"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.4.0"
  },
  "private": true
}
